compoundLine = {
  \transpose c c  { \pattern }
  \transpose c es   { \pattern }
  \transpose c ges  { \pattern }
  \transpose c a    { \pattern }
  % \transpose c ges  { \pattern }
  % \transpose c es   { \pattern }
  % \transpose c c    { \pattern }
}